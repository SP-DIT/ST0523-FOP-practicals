// q1
//
// Fix the error in the code below that is supposed to evaluate 48 / 2 (9 + 3)
//
function evaluate() {
    return 48 / 2(9 + 3);
}

// q2
//
// Add brackets to the code below to fix the error in the code below that is supposed to squared the value 3 times.
//
function square3Time(x) {
    return x ** (2 ** (2 ** 2));
}

// q3
//
// Fix the error in the code below:
//
function add(x, y) {
    x + y;
}
function multiplyBy3(x) {
    return add(x, add(x, x));
}

// q4
//
// Fix the error in the code below:
function greetings(name) {
    const message = 'Hello, ';
    message = message + name;
    return message;
}

// q5
//
// Fix the error in the code below:
function displayName(firstName, lastName) {
    const full name = firstName + ' ' + lastName;
    return full name
}
